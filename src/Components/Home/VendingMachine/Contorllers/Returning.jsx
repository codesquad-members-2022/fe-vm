import styled from 'styled-components';

const ReturningDiv = styled.div`
	padding: 20px;
	font-size: 20px;
`;

const Returning = () => {
	return <ReturningDiv>반납</ReturningDiv>;
};

export default Returning;
