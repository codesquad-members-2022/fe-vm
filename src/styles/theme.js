const calcRem = (size) => `${size / 16}rem`;

const colors = {
  black: "#1b1b1b",
  gray1: "#3f3f3f",
  gray2: "#777777",
  gray3: "#bcbcbc",
  gray4: "#ebebeb",
  offWhite: "#f8f7f7",
  white: "#ffffff",
  baeMint: "#2ac1bc",
};

const theme = { colors };

export default theme;
